import { Plugin } from "esbuild";
import { LoadOptions } from "js-yaml";
interface YamlPluginOptions {
    loadOptions?: LoadOptions;
    transform?: (data: string | number | object, filePath: string) => object | undefined;
}
export declare const yamlPlugin: (options: YamlPluginOptions) => Plugin;
export {};
